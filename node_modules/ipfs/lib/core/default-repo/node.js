'use strict';

var os = require('os');
var fs = require('fs-blob-store');
var IPFSRepo = require('ipfs-repo');
var path = require('path');

module.exports = function (dir) {
  var repoPath = dir || path.join(os.homedir(), '.ipfs');
  return new IPFSRepo(repoPath, { stores: fs });
};