'use strict';

var idb = require('idb-plus-blob-store');
var IPFSRepo = require('ipfs-repo');

module.exports = function (dir) {
  var repoPath = dir || 'ipfs';
  return new IPFSRepo(repoPath, { stores: idb });
};