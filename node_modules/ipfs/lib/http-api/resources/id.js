'use strict';

var boom = require('boom');

exports = module.exports;

exports.get = function (request, reply) {
  request.server.app.ipfs.id(function (err, id) {
    if (err) {
      return reply(boom.badRequest(err));
    }
    return reply(id);
  });
};